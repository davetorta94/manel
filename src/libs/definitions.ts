

export type Category = {
    id: number;
    name: string;
    example: string;
    link: string;
    
}

export type Social = {
    id: string;
    name: string;
    link: string;
    color: string;
}

export type Video = {
    id: number,
    name: string,
    url: string,
    thumb: string,
}


export type Category = {
    id: number;
    name: string;
    example_link: string;
    list_link: string;
    
}

export type Social = {
    id: string;
    name: string;
    link: string;
    color: string;
}

export type Video = {
    id: number,
    name: string,
    link: string,
    thumb: string,
}